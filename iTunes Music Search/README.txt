This Android application allows the user to search for a song on iTunes.

This program uses Apples API to get a list of results.

The music is stored in an array list of music objects where it can be sorted by date or price.

The music is displayed in a list.